/**
 * Testnet Configuration
 *
 * Simulation mode is DISABLED.
 * The app uses the real Pi SDK and real backend endpoints exclusively.
 */

export function isTestnetMode(): boolean {
  return false;
}
